<%= form_for(@game_session) do |f| %>
  <% if @game_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_session.errors.count, "error") %> prohibited this game_session from being saved:</h2>

      <ul>
      <% @game_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <select name="game_id">
      <%= @games.each do |game| %>
        <option value="<%= game.id %>"><%= game.game_title %></option>
      <% end %>
    </select>
  </div>
  <div class="field">
    <select name="game_id">
      <%= @games.each do |game| %>
        <option value="<%= game.id %>"><%= game.game_mode %></option>
      <% end %>
    </select>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
